/**
 * Vitest global test setup.
 * Runs before every test file — imports jest-dom matchers so assertions like
 * `expect(el).toBeInTheDocument()` are available everywhere.
 */
import '@testing-library/jest-dom'
